import { reverse } from "./reverse"

describe("004.recursion", () => {
  describe("reverse", () => {
    test('should be true', () => {
      expect(reverse('awesome')).toEqual('emosewa')
    })
  })
})